This version is used for hard linking with the Ad0pt Net Zero cluster model. Because the pycharm-based linking procedure does not allow changes within the AIMMS user interface, we use two copies of the same model (_PolicyPath and _FossilPhaseout), which are identical except for their default input data file, to make the different scenario links.
